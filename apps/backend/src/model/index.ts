import UserModel, { IUser } from "./user.model";
import ShortUrlModel, { IShortUrl } from "./short-url.model";
import ClickStatModel, { IClickStat } from "./click-stat.model";

export {
    UserModel,
    IUser,
    ShortUrlModel,
    IShortUrl,
    ClickStatModel,
    IClickStat,
};
